spring.datasource.url=jdbc:postgresql://localhost:5432/tollway
spring.datasource.username=postgres
spring.datasource.password=password321

spring.datasource.initialization-mode=always

spring.jpa.hibernate.ddl-auto=none

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jpa.show-sql=true
spring.thymeleaf.cache=false
version=dev

# TRACE -> DEBUG (application) -> INFO -> WARN -> ERROR (major)
logging.level.root=WARN
logging.level.dev.ericyao.tollway=DEBUG
logging.level.org.springframework=INFO
# use the following configuration
logging.file=app.log
#logging.pattern.dateformat=yy-mm-dd

#server.error.whiltelabel.enabled=false

# we need the below property to turn off hibernate auto validation
# we are using constraints validation and customizing our data validation
# rules
#spring.jpa.properties.javax.persistence.validation.mode=none